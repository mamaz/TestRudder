# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'TestRudder' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Rudder', :git => 'https://github.com/feedloop/rudder-sdk-ios.git', :tag => 'v1.0.25-feedloop'

  # Pods for TestRudder
  target 'TestRudderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestRudderUITests' do
    # Pods for testing
  end

end
